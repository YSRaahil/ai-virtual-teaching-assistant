[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Tests that run without server (no HTTP)
    integration: Tests that require server running
    slow: Tests that make LLM API calls